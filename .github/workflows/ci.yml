name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      # TEMPORARY — keeps the run "in progress" long enough to watch the Test
      # Radar tree auto-refresh through its states. Remove before merging.
      - name: (temp) slow down to demo auto-refresh
        run: sleep 120

      - name: Type check
        run: npm run check-types

      - name: Lint
        run: npm run lint

      - name: Build
        run: node esbuild.js --production

      - name: Unit tests
        run: npm run test:unit

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: test-results/junit.xml
